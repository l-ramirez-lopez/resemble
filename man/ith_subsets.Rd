% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/funlib.R
\name{ith_subsets}
\alias{ith_subsets}
\title{Iterator for Subsets by Index}
\usage{
ith_subsets(x, y, kindx, D = NULL)
}
\arguments{
\item{x}{A matrix or data frame of predictors (rows = observations).}

\item{y}{A vector or matrix of responses (same rows as \code{x}).}

\item{kindx}{A list or matrix of row indices (e.g., nearest neighbors).}

\item{D}{Optional square dissimilarity matrix matching rows of \code{x}.}
}
\value{
An iterator object of class \code{isubset}, \code{abstractiter}, \code{iter}. Each
call to \code{nextElem()} yields:
\itemize{
\item \code{x}: A subset of \code{x}, optionally prepended with dissimilarities.
\item \code{y}: Corresponding subset of \code{y}, with NA rows removed.
}
}
\description{
Internal helper to iterate over subsets of \code{x} and \code{y} based on \code{kindx}
indices. Optionally augments \code{x} with a dissimilarity matrix \code{D} and
filters out rows where \code{y} is NA.
}
\details{
For each iteration, indices from \code{kindx} are used to extract rows
from \code{x} and \code{y}. If \code{D} is given, \code{x} is augmented with local
dissimilarities from \code{D[knns, knns]}. Observations where \code{y} is NA
are excluded from both \code{x} and \code{y} in the returned subset.
}
\author{
Leonardo Ramirez-Lopez
}
\keyword{internal}
