% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local_helpers.R
\name{get_plsr}
\alias{get_plsr}
\title{Cross-validated PLS fitting for gesearch}
\usage{
get_plsr(
  X,
  Y,
  indices = NULL,
  fit_method,
  number = NULL,
  group = NULL,
  retrieve = FALSE,
  tune = TRUE,
  seed = NULL,
  p = NULL
)
}
\arguments{
\item{X}{Numeric matrix of predictor variables (observations in rows).}

\item{Y}{Numeric matrix or vector of response values.}

\item{indices}{Optional integer vector specifying row indices to use for
training. If \code{NULL}, all rows are used.}

\item{fit_method}{A \code{fit_method} object created by \code{\link[=fit_pls]{fit_pls()}} or
\code{\link[=fit_wapls]{fit_wapls()}} specifying the PLS fitting parameters.}

\item{number}{Integer. Number of cross-validation iterations.}

\item{group}{Optional factor assigning group labels to observations for
leave-group-out cross-validation.}

\item{retrieve}{Logical. If \code{TRUE}, return resampling indices.}

\item{tune}{Logical. If \code{TRUE}, tune the number of PLS components via
cross-validation.}

\item{seed}{Optional integer for reproducible resampling.}

\item{p}{Numeric. Proportion of observations to retain in each
cross-validation fold.}
}
\value{
An object of class \code{"plsr"} containing:
\describe{
\item{models}{Fitted PLS model from \code{opls_get_basics()}.}
\item{cv_results}{Cross-validation results.}
\item{train_resamples}{Resampling indices (if \code{retrieve = TRUE}).}
\item{fit_method}{The \code{fit_method} object used.}
\item{Xscale}{Scaling parameters for predictors.}
\item{call}{The matched call.}
}
}
\description{
Internal function that fits a PLS model with cross-validation for use in
\code{gesearch()}. Handles both \code{fit_pls()} and \code{fit_wapls()} methods.
}
\seealso{
\code{\link[=gesearch]{gesearch()}}, \code{\link[=fit_pls]{fit_pls()}}, \code{\link[=fit_wapls]{fit_wapls()}}
}
\author{
Leonardo Ramirez-Lopez
}
\keyword{internal}
